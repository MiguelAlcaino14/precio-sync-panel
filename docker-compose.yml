services:

  panel:
    build: .
    ports:
      - "5173:5173"
    environment:
      API_URL: http://host.docker.internal:3001
    volumes:
      - .:/app
      - /app/node_modules
    restart: unless-stopped
